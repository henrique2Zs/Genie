import { ContentUneditableDirective } from './content-uneditable.directive';

describe('ContentUneditableDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentUneditableDirective();
    expect(directive).toBeTruthy();
  });
});
