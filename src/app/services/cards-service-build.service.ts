import { Injectable } from '@angular/core';
import { Card } from '../class/card';
import { CardsService } from './cards.service';

@Injectable({
  providedIn: 'root'
})
export class CardsServiceBuild{
/*
  cardsBuilt: number[] = new Array<number>();
  cardsBuiltcard: Card[] = new Array<Card>();
  /*
  cards: Card[];
  selectedCard: Card;
*/
/*
  constructor(private cardsService: CardsService) {    
    this.cardsBuilt.push(2);
    console.log("ServiceBuild: iniciar getBuiltCards")
    this.getBuiltcards();  
    console.log("ServiceBuild: finalizado getBuildCards")
   }

   getBuiltcards() {
     console.log("ServiceBuild: getBuilcards..");
     let lenght = this.cardsBuilt.length;
     for (let num = 0; num < (this.cardsBuilt.length); num++) {
        this.cardsBuiltcard.push(this.cardsService.cards.get(this.cardsBuilt[num]));       
    console.log("ServiceBuild: pushing of num: " + num)
   } */
  }
/*
  
  public getCards(): Card[] {
      return super.getCards()
  }

  
  public put(card: Card): void {
      super.put(card)
  }
*/

